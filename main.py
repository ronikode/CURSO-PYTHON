from utils.customer import Customer
from utils.functions import sum_numbers
from datetime import timedelta, timezone

c = Customer(name="", last_name="", dni="")
print(c)
result = sum_numbers(10, 10)
print(result)
